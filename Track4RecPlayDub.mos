!autoload
!name Track 4 Rec/Play


Track 4
!multiclick 500

if mode == reset
Record
end

else if mode == mute
Reset
Record
end

else if mode == record
Play
end

else if mode == play
Overdub
end

else if mode != reset
Overdub
end
endif

label click
Overdub
end
