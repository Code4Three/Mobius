!autoload
!name Track 2 Rec/Play

message Layer count is $layerCount

Track 2
!multiclick 500

if mode == reset
Record
end

else if mode == mute
Reset
Record
end

else if mode == record
Play
end

else if mode == play
Overdub
end

else if mode != reset
Overdub
end
endif

label click
Overdub
end
